# =============================================================================
# proxy.env.example - VPN Proxy Configuration Template
# =============================================================================
# Copy this file to /root/proxy.env and configure your VPN settings.
# If you don't need VPN proxy, set PROXY_ENABLED=false
# =============================================================================

# Enable or disable VPN proxy routing (true/false)
PROXY_ENABLED=true

# Your VPN server's IP address (get this by pinging your VPN server)
# This is excluded from routing to prevent connection loops
VPN_SERVER_IP=YOUR_VPN_SERVER_IP_HERE

# Your VPS's public IP address (for verification)
VPS_PUBLIC_IP=YOUR_VPS_IP_HERE

# Redsocks local port (default: 12345)
REDSOCKS_PORT=12345

# Xray SOCKS proxy port (default: 10808)
XRAY_SOCKS_PORT=10808
