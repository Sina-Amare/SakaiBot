# Core dependencies
telethon>=1.30.0
cryptg>=0.4.0

# AI and ML
openai>=1.0.0
google-generativeai>=0.1.0

# Speech processing
SpeechRecognition>=3.10.0
pydub

# Text-to-Speech (Google Gemini TTS)
google-genai>=0.8.0

# NLP - using simpler alternatives to avoid compilation issues
# hazm dependency removed due to numpy compilation issues on Windows
# Instead using simpler alternatives or removing for now

# Async utilities
aiofiles>=23.0.0

# Configuration and validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Utilities
pytz>=2023.3
click
rich

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
